<?xml version="1.0" encoding="utf-8"?>
<!--
    Copyright (C) 2017 dd2568 : Project submitted for Udacity Android Developer Nanodegree.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uadnd.cou8901.popularmoviesapp1">

    <!-- This permission is needed for getting Movie DB data from internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- Manifest entry needed for Movie DB Main Activity -->
        <activity
            android:name=".MovieDbMainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <!-- Manifest entry needed for Movie DB Detail Activity -->
        <activity android:name=".MovieDbDetailActivity"
            android:label="Movie Detail">

        </activity>
    </application>

</manifest>